# rget

rget是一个支持指纹伪装，断点续传，自定义线程，智能连接复用，可视化下载显示，批量下载，文件校验，纯Rust，高可自定义，高性能异步的cli下载器

同时，他对文件存储模式进行了优化，通过单文件异步写入，他比IDM等分块下载软件造成硬盘损耗少一倍

再次，作为一款0配置无依赖的下载器，在无出错的情况下，可以做到0文件残留

又一次，指纹伪装是该软件的特色差分，可以解决一些冷门难题
